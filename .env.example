# Google sign-in (optional). Create an OAuth client ID of type "Web application" at
# https://console.cloud.google.com/apis/credentials and add http://localhost:5173
# (and http://localhost:5174 for `npm start`) under Authorized JavaScript origins.
GOOGLE_CLIENT_ID=
